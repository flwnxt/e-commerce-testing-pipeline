# Test dependencies — install alongside requirements.txt for local development
# These are also installed in docker/Dockerfile.test

playwright==1.51.0
pytest==8.3.5
pytest-playwright==0.7.0
pytest-html==4.1.1
locust==2.34.1
